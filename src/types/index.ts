// TypeScript interfaces — stub for Story 1.1
// Every Rust IPC struct must have a matching interface here.
// serde rename_all = "camelCase" handles the naming bridge.

// TODO: populate with ConnectionProfile, SchemaData, etc. in Story 1.2+
